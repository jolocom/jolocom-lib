import { SignedCredential } from '../signedCredential/signedCredential'

/* Collection of methods to aid Signed Credential Parsing */

export const SignedCredentialParser = {
  fromJSON : SignedCredential.fromJSON
}
