import { expect } from 'chai'
import testData from './data/identity'
import EthereumResolver from '../ts/ethereum/ethereumResolver'

describe('Ethereum Resolver', () => {
  const ethResolver = new EthereumResolver()

  it('Should correctly query contract for the user\'s DDO hash', () => {

  })

  it('Should return error in case reading record fails', () => {
  
  })

  it('Should correctly register a user\'s DDO hash', () => {

  })

  it('Should return error in case writting record fails', () => {
  
  })
})
